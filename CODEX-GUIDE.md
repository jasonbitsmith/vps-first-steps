# 用 Codex 引导初始化 VPS

## 最容易开始的方式

在 Codex 打开这个项目目录，然后发送：

> 阅读 SKILL.md，帮我初始化一台闲置 VPS。先引导我连接，检查是否有现有服务，再完成基础配置和验收，保留现在的 SSH 登录方式。

不用先安装 Skill，也无需逐条复制安装命令。Codex 是否能自动执行取决于当前环境是否提供终端、网络访问和可用 SSH 认证；仅在浏览器里打开 GitHub 页面不会自动赋予服务器访问权限。

想复用为个人 Skill，可以让 Codex 将本项目安装到它支持的个人 skills 目录，安装后按当前客户端要求刷新技能列表，再使用 `$vps-first-steps` 发起任务。不要把服务器密码写入 Skill。

## Codex 会向你询问什么？

| 信息 | 示例 |
| --- | --- |
| SSH 别名或服务器地址 | `my-test-vps`，或服务商提供的 IP |
| SSH 用户名和端口 | `root`、`22`，以实际配置为准 |
| 登录方式 | 已配置的 SSH 密钥/agent，或交互密码登录 |
| 使用情况 | 全新空机，或装过软件但目前不用 |

密码在可信登录界面输入，不要发到聊天里；私钥只提供本地路径（如确有需要），不粘贴文件内容。

## 连接示例（供 agent 参考）

优先使用已配置别名。以下 `my-test-vps` 为示例，不可直接假定存在：

```bash
ssh my-test-vps
```

显式地址连接需要用户提供用户名和端口。不要用 root 或 22 覆盖用户已有设置。密码认证需要交互终端；BatchMode 只能用于检测是否有可用的非交互认证。

远程只读检查可逐条执行：

```bash
cat /etc/os-release
id
free -h
df -h /
swapon --show
ss -lntup
systemctl is-active ssh
```

非 root 需要适当 sudo 权限才可读取完整服务信息：

```bash
sudo sshd -t
sudo sshd -T
sudo ufw status verbose
```

只保留与检查相关的输出，不公开原始 SSH 配置、监听地址或环境变量。

上传流程：远程执行 `mktemp -d` → 本地 scp 上传随附脚本 → 两端 `sha256sum`（Mac 可用 `shasum -a 256`）比较 → 远程 `--check` → 初始化 → `--status`。用本次返回的真实目录替换示例，不猜路径。

独立连接验收可对同一已验证目标使用以下 SSH 选项，禁用连接复用：

```bash
ssh -o ControlMaster=no -o ControlPath=none my-test-vps
```

新连接里验证身份及管理员权限。root 用 `id -u`；非 root 用 `sudo -n true`，若需密码则在交互终端执行 `sudo true`。不要为通过检查而更改 sudo 策略。

## 遇到问题

- **连接超时**：检查服务商状态、地址、端口与安全组，不自动重装系统。
- **认证失败**：确认账号和认证方式，不从终端软件提取保存的密码。
- **主机密钥变化**：核实是否重装或换机，不用关闭校验绕过。
- **发现已有服务**：先确定是否需保留；默认 UFW 只新增 SSH 规则。
- **包管理被占用**：检查正在运行的更新任务，不删除锁文件或强杀 apt。
- **步骤失败**：保留会话，定位失败项，不反复执行整个初始化。
- **状态命令报告异常**：命令能运行不等于全部通过，按组件分析。

## 能力和验证边界

本 Skill 面向已有 VPS 的基础初始化，不购买、重装服务器，不部署 aaPanel、3x-ui、WARP 或网站。

默认初始化的有限实机结果见 [TESTING.md](TESTING.md)。此文新增的 Codex 自动连接/上传流程尚未完整实机验证；不能宣称所有客户端都能“一键成功”。首次认证、权限提示和个别登录验证仍可能需要用户参与。
