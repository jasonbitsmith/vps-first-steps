# 测试记录

## 2026-09-16：默认初始化流程

证据来源：用户提供的 FinalShell 输出截图，以及用户明确确认新连接成功。

- 环境：Ubuntu 26.04 LTS，x86_64，约 1 GB 内存。
- 机器已有软件和约 545 MB swap，不能代表全新系统测试。
- `--check` 通过，识别 SSH 端口 22。
- 默认初始化执行到完成。
- UFW active，IPv4/IPv6 均放行 22/tcp。
- fail2ban active，sshd jail 可查询。
- 保留原有 swap，未创建 2 GB swap。
- 用户确认第二个 SSH 连接登录成功。

## 此次发现与修正

旧汇总错误显示防火墙/fail2ban 为 unchanged，并把 swap 目标大小当作实际大小。新版改为读取实际状态，同时增加只读 `--status`。

本次状态汇总修正已做本地模拟测试，尚未在 VPS 上执行新版 `--status`。安装步骤没有因本次修改重跑。

## 尚未验证

- 重启后的登录和服务状态。
- 自动安全更新是否实际下载并安装更新。
- 全新 Ubuntu/Debian 安装、新建 swap 和高级 SSH 选项。
- 原有 x-ui 等业务是否仍可访问；当前 UFW 仅放行 SSH。

测试记录不包含服务器 IP、账号凭据或原始日志。
