---
name: vps-first-steps
description: 引导新手通过 Codex 连接已有 Ubuntu/Debian VPS，自动完成基础初始化和状态验收。用于服务器首次配置、安装基础工具、UFW、fail2ban、swap 与自动安全更新；不负责购买 VPS、重装系统或部署面板、网站和代理节点。
---

# VPS 起手式 · Codex 引导初始化

目标：让用户用一句话发起任务，由 agent 完成已获授权的连接、检查、执行和验收。先读随附的 `vps-init.sh`；需要连接示例或故障处理时读 [CODEX-GUIDE.md](CODEX-GUIDE.md)。README 面向用户，不必重复要求用户手动运行其中每条命令。

## 1. 收集最少信息

从当前会话取得已有信息，只询问缺失内容：

- SSH 配置别名，或服务器地址、SSH 端口和登录用户名。
- 可用登录方式：已有 SSH agent/本地密钥，或用户可操作的密码登录终端。
- 是否闲置、是否有需要保留的业务，以及服务商网页控制台是否可用。

优先复用用户指定的 SSH 别名和密钥，不读取私钥内容、FinalShell 密码库或历史记录。不要把密码放进命令参数、日志或聊天。无认证途径时解释具体缺项，引导用户在可信登录界面完成认证；不要未经确认安装新公钥或更改认证配置。

## 2. 建立连接并做只读预检

在操作者本地用 SSH 连接指定目标，保留会话。首次连接按宿主工具的主机密钥验证流程处理；已有主机密钥变化时停止核实，不关闭验证。

检查系统版本、当前用户和 sudo、内存、磁盘、swap、SSH 有效端口与实际连接端口、监听服务和 UFW 现状。常用只读命令见 CODEX-GUIDE.md。

- 确认目标为 Ubuntu/Debian + apt + systemd，不凭主机名或用户描述猜测。
- 用户说闲置不等于全新：检测到网站、x-ui、Docker、数据库或其他非基础服务时，说明默认防火墙可能阻断这些服务，先确定需保留的端口；未经明确范围决定不运行默认初始化。
- 若只有 sudo 密码交互可用，保持交互终端；不要假定免密码 sudo 可用。
- 远程权限、磁盘空间、系统版本或端口检测有问题，先解决该问题。不要把 --check 通过说成完整部署兼容性保证。

## 3. 准备同一份脚本

使用当前 skill 目录的 `vps-init.sh`，不要执行网络返回的任意替代脚本。用远程 `mktemp -d` 创建本次任务目录，通过 scp 上传；核对两端 SHA-256。远程目录创建、上传和后续命令均指向同一台主机、同一 SSH 端口和账号。

路径和参数必须正确 shell 引用，不拼接未校验用户输入。不要在本地 Mac/Windows 上运行初始化脚本。

先在目标服务器运行 `bash <远程脚本路径> --check`（非 root 使用 sudo）。通过后给出简短变更摘要：系统更新、工具、UFW、fail2ban、swap、自动更新及保留现有 SSH 的默认行为。

## 4. 执行授权范围

用户已明确授权初始化、目标范围已确定且工具允许时继续，不重复索要同一授权。只有检查请求时停在检查结果。

- 默认不带创建用户、关闭密码或关闭 root 的参数，也不改 SSH 端口。
- 普通初始化可在授权明确时使用 `--yes`，或在交互终端回应普通确认。
- 用户主动要求新管理账号时，解释免密码 sudo 权限，使用其自己的有效公钥。
- 限制旧登录方式时遵循脚本第二会话验证；不能代用户猜测或伪造 `VERIFIED`。此流程不能用 `--yes` 跳过。
- 长任务保持连接并报告进度，失败时检查最后失败步骤和已完成改动。不要自动重跑整个脚本或宣称全局回滚。

## 5. 自动验收

在目标服务器运行相同脚本的 `--status`，根据实际输出逐项判断，不能仅凭退出码 0 或“初始化完成”断言通过。检查 UFW SSH 放行、fail2ban active、sshd jail、实际 swap；跳过的组件按跳过记录。

另建一次独立 SSH 连接，验证新登录和管理员权限，避免复用旧 ControlMaster 连接。自动连接不可用时才让用户执行这一步，明确区分用户确认与 agent 实测。

只读检查 `/var/run/reboot-required` 是否存在。不要自动重启；新连接成功不代表重启后配置已验证。不要为了测试防护而故意触发封禁。

## 6. 用新手能看懂的方式交付

给出：执行环境、完成项、失败/跳过项、登录是否验证、实际 swap、是否建议安排重启、远程脚本路径及一条可复用的 `--status` 命令。

说明测试覆盖范围。既有软件机器不能记为全新系统；不把初始化等同于面板或代理节点部署。公开测试记录只写系统和结果，不包含服务器 IP、凭据或未经脱敏的原始日志。更新 GitHub 需属于用户授权范围。
